
<%- include("partials/header"); -%>

<div class="container">
    <div class="col-lg-8 col-sm-10">
        <%  posts.map((post)=>{ %>
            <h1 class="fs-3"><%=post.title%></h1> 
            <% if (post.author) { %> <em>di <%=post.author%></em> <% } else { %> <em>di Anonimo </em> <% } %>
            <p>
            <%=post.content.substring(0, 100) + " ..."%>
            <br> <a class="btn btn-outline-dark btn-sm brand-btn my-2" href="/posts/<%=post._id%>">Leggi di più</a>
            </p>
            <hr>
        
            <% }) %>
    </div>
</div>


<%- include("partials/footer"); -%>
